package com.testjava.prices.consumers;

/*
 * @Component public class PriceConsumer {
 * 
 * @Autowired private PriceService priceService;
 * 
 * public void receivePrice(String jsonPrice) { ObjectMapper mapper = new
 * ObjectMapper();
 * 
 * try { priceService.save(mapper.readValue(jsonPrice, PriceDto.class)); } catch
 * (Exception ex) { } }
 * 
 * }
 */
